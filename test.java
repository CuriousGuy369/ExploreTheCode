class Solution {

    boolean isOpen(char c){
        char[] array = {'(','[','}'};
        for(char i : array){
            if(i(i)) return true;
        }
    }
    public boolean isValid(String s) {
        System.out.println(isOpen('('));
        return true;
    }
}